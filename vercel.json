{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/jobs/:path*", "destination": "/api/index" },
    { "source": "/jobs",        "destination": "/api/index" },
    { "source": "/health",      "destination": "/api/index" }
  ]
}
